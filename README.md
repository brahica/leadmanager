Install Python Env :

    $ pipenv install
    $ pipenv shell

Install Npm Modules :

    $ npm install

Build JS for Dev : 

    $ npm run dev

Run Django Server for dev : 

    $ ./leadmanager/manage.py runserver
